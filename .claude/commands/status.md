Quick status check (keep it brief):

1. Current task we're working on
2. Last action taken
3. Build status: `gh run list -L1 --json status,conclusion,databaseId,name -q '.[]'`
4. If in_progress, which step: check output
5. Next step when complete

Format: 5 lines max, no preamble.
Then continue working (don't wait for input).