name: CI-Windows-Build-Test-v2

# Thin caller for unified build-test.yml
# Tests the new reusable workflow architecture

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.github/docs/**'
      - '.claude/**'
      - 'docs/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.github/docs/**'
      - '.claude/**'
      - 'docs/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    uses: ./.github/workflows/build-test.yml
    with:
      os: windows
      runner: windows-latest
      cuda: 'ON'
      cuda_version: '11.8.0'
      build_type: Release
    secrets: inherit
